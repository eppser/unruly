# Answers 200 with a JSON object to every path. An API gateway with a catch-all
# route, or a health-check shim.
server {
  listen 80;
  location / {
    default_type application/json;
    return 200 '{"status":"ok","service":"gateway"}';
  }
}
