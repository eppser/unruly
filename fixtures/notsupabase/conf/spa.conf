# A single-page application. Unknown routes return index.html with 200, which
# is how every React/Vue/Svelte deployment on the planet is configured.
server {
  listen 80;
  root /usr/share/nginx/html;
  location / {
    try_files $uri /index.html;
    add_header Content-Type text/html;
  }
}
