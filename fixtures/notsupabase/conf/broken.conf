# Answers 500 to everything. An origin that is down, which must read as
# "could not measure" rather than as a clean project.
server {
  listen 80;
  location / {
    default_type application/json;
    return 500 '{"error":"internal"}';
  }
}
