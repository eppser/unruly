-- Project 09: 120 relations, three name families, OpenAPI turned off.
--
-- GENERATED BY schema/gen.py. Do not edit by hand; edit the generator and
-- regenerate, or the counts the answer key asserts stop meaning anything.
--
-- The one property this target measures: discovery RECALL as a percentage.
-- Twenty of these names are in every wordlist, twenty are contentless
-- placeholders, and eighty cannot be guessed at all. A scanner that enumerates
-- by dictionary has a hard ceiling here and the ceiling is visible in the
-- score.
--
-- Exposure is anti-correlated with guessability on purpose: 14 of the 20
-- guessable names are protected, and half the unguessable ones are open and
-- hold credential-, card- and PII-shaped columns.


GRANT USAGE ON SCHEMA public TO anon, authenticated;

-- guessable / open / plain / 4 rows
CREATE TABLE public."users" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."users" (label, value)
SELECT 'users-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- guessable / norights / plain / 5 rows
CREATE TABLE public."profiles" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."profiles" (label, value)
SELECT 'profiles-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- guessable / rls_silent / plain / 6 rows
CREATE TABLE public."orders" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."orders" (label, value)
SELECT 'orders-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- guessable / rls_silent / plain / 7 rows
CREATE TABLE public."sessions" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."sessions" (label, value)
SELECT 'sessions-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- guessable / rls_silent / plain / 8 rows
CREATE TABLE public."payments" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."payments" (label, value)
SELECT 'payments-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- guessable / rls_silent / plain / 4 rows
CREATE TABLE public."messages" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."messages" (label, value)
SELECT 'messages-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- guessable / norights / plain / 5 rows
CREATE TABLE public."accounts" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."accounts" (label, value)
SELECT 'accounts-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- guessable / norights / plain / 6 rows
CREATE TABLE public."customers" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."customers" (label, value)
SELECT 'customers-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- guessable / open / plain / 7 rows
CREATE TABLE public."products" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."products" (label, value)
SELECT 'products-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- guessable / norights / plain / 8 rows
CREATE TABLE public."invoices" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."invoices" (label, value)
SELECT 'invoices-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- guessable / norights / plain / 4 rows
CREATE TABLE public."subscriptions" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."subscriptions" (label, value)
SELECT 'subscriptions-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- guessable / norights / plain / 5 rows
CREATE TABLE public."notifications" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."notifications" (label, value)
SELECT 'notifications-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- guessable / open / plain / 6 rows
CREATE TABLE public."comments" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."comments" (label, value)
SELECT 'comments-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- guessable / open / plain / 7 rows
CREATE TABLE public."posts" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."posts" (label, value)
SELECT 'posts-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- guessable / open / plain / 8 rows
CREATE TABLE public."files" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."files" (label, value)
SELECT 'files-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- guessable / norights / plain / 4 rows
CREATE TABLE public."teams" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."teams" (label, value)
SELECT 'teams-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- guessable / norights / plain / 5 rows
CREATE TABLE public."projects" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."projects" (label, value)
SELECT 'projects-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- guessable / norights / plain / 6 rows
CREATE TABLE public."tickets" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tickets" (label, value)
SELECT 'tickets-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- guessable / norights / plain / 7 rows
CREATE TABLE public."addresses" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."addresses" (label, value)
SELECT 'addresses-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- guessable / open / plain / 8 rows
CREATE TABLE public."events" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."events" (label, value)
SELECT 'events-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- generic / open / plain / 2 rows
CREATE TABLE public."t1" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t1" (label, value)
SELECT 't1-' || i, 'value-' || i FROM generate_series(1, 2) i;
-- generic / rls_silent / plain / 3 rows
CREATE TABLE public."t2" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t2" (label, value)
SELECT 't2-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- generic / open / plain / 4 rows
CREATE TABLE public."t3" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t3" (label, value)
SELECT 't3-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- generic / norights / plain / 5 rows
CREATE TABLE public."t4" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t4" (label, value)
SELECT 't4-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- generic / open / plain / 2 rows
CREATE TABLE public."t5" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t5" (label, value)
SELECT 't5-' || i, 'value-' || i FROM generate_series(1, 2) i;
-- generic / rls_silent / plain / 3 rows
CREATE TABLE public."t6" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t6" (label, value)
SELECT 't6-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- generic / open / plain / 4 rows
CREATE TABLE public."t7" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t7" (label, value)
SELECT 't7-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- generic / norights / plain / 5 rows
CREATE TABLE public."t8" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t8" (label, value)
SELECT 't8-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- generic / open / plain / 2 rows
CREATE TABLE public."t9" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t9" (label, value)
SELECT 't9-' || i, 'value-' || i FROM generate_series(1, 2) i;
-- generic / rls_silent / plain / 3 rows
CREATE TABLE public."t10" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t10" (label, value)
SELECT 't10-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- generic / open / plain / 4 rows
CREATE TABLE public."t11" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t11" (label, value)
SELECT 't11-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- generic / norights / plain / 5 rows
CREATE TABLE public."t12" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."t12" (label, value)
SELECT 't12-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- generic / open / plain / 2 rows
CREATE TABLE public."data" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."data" (label, value)
SELECT 'data-' || i, 'value-' || i FROM generate_series(1, 2) i;
-- generic / rls_silent / plain / 3 rows
CREATE TABLE public."payload" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."payload" (label, value)
SELECT 'payload-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- generic / open / plain / 4 rows
CREATE TABLE public."tmp_1" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tmp_1" (label, value)
SELECT 'tmp_1-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- generic / norights / plain / 5 rows
CREATE TABLE public."tmp_2" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tmp_2" (label, value)
SELECT 'tmp_2-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- generic / open / plain / 2 rows
CREATE TABLE public."staging_a" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."staging_a" (label, value)
SELECT 'staging_a-' || i, 'value-' || i FROM generate_series(1, 2) i;
-- generic / rls_silent / plain / 3 rows
CREATE TABLE public."staging_b" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."staging_b" (label, value)
SELECT 'staging_b-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- generic / open / plain / 4 rows
CREATE TABLE public."misc" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."misc" (label, value)
SELECT 'misc-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- generic / norights / plain / 5 rows
CREATE TABLE public."tbl" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl" (label, value)
SELECT 'tbl-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / open / credential / 3 rows
CREATE TABLE public."tbl_0e1eb483" (
  id bigserial PRIMARY KEY,
  email text NOT NULL,
  password_hash text NOT NULL,
  api_key text NOT NULL,
  totp_secret text
);
INSERT INTO public."tbl_0e1eb483" (email, password_hash, api_key, totp_secret)
SELECT 'user' || i || '@example.invalid',
       '$2b$12$' || lpad(i::text, 53, '0'),
       'sk_test_' || md5('tbl_0e1eb483' || i),
       'JBSWY3DPEHPK3PX' || i
FROM generate_series(1, 3) i;
-- unguessable / rls_silent / plain / 4 rows
CREATE TABLE public."tbl_75bea5ae" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_75bea5ae" (label, value)
SELECT 'tbl_75bea5ae-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / open / plain / 5 rows
CREATE TABLE public."tbl_685ec957" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_685ec957" (label, value)
SELECT 'tbl_685ec957-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / norights / plain / 6 rows
CREATE TABLE public."tbl_a316d581" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_a316d581" (label, value)
SELECT 'tbl_a316d581-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- unguessable / open / plain / 7 rows
CREATE TABLE public."tbl_8bd78e19" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_8bd78e19" (label, value)
SELECT 'tbl_8bd78e19-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- unguessable / rls_silent / plain / 8 rows
CREATE TABLE public."tbl_8f676a9d" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_8f676a9d" (label, value)
SELECT 'tbl_8f676a9d-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- unguessable / open / card / 9 rows
CREATE TABLE public."tbl_1c6d981c" (
  id bigserial PRIMARY KEY,
  card_number text NOT NULL,
  card_expiry text,
  iban text,
  billing_email text
);
INSERT INTO public."tbl_1c6d981c" (card_number, card_expiry, iban, billing_email)
SELECT (ARRAY['4111111111111111','5555555555554444','378282246310005','6011111111111117'])[1 + (i % 4)],
       '0' || (1 + (i % 9)) || '/29',
       (ARRAY['DE89370400440532013000','GB82WEST12345698765432','NL91ABNA0417164300'])[1 + (i % 3)],
       'billing' || i || '@example.invalid'
FROM generate_series(1, 9) i;
-- unguessable / norights / plain / 3 rows
CREATE TABLE public."tbl_f0e2fb5b" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_f0e2fb5b" (label, value)
SELECT 'tbl_f0e2fb5b-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / open / plain / 4 rows
CREATE TABLE public."tbl_3d461043" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_3d461043" (label, value)
SELECT 'tbl_3d461043-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / rls_silent / plain / 5 rows
CREATE TABLE public."tbl_f412a711" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_f412a711" (label, value)
SELECT 'tbl_f412a711-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / open / plain / 6 rows
CREATE TABLE public."tbl_4c521ea9" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_4c521ea9" (label, value)
SELECT 'tbl_4c521ea9-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- unguessable / norights / plain / 7 rows
CREATE TABLE public."tbl_efad9476" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_efad9476" (label, value)
SELECT 'tbl_efad9476-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- unguessable / open / pii / 8 rows
CREATE TABLE public."tbl_beef7050" (
  id bigserial PRIMARY KEY,
  full_name text NOT NULL,
  email text NOT NULL,
  phone_number text,
  national_id text
);
INSERT INTO public."tbl_beef7050" (full_name, email, phone_number, national_id)
SELECT (ARRAY['Dana Whitfield','Marcus Oyelaran','Priya Raghunathan','Tomas Berg','Elena Marchetti'])[1 + (i % 5)] || ' ' || i,
       'person' || i || '@example.invalid',
       '+1-555-02' || lpad(i::text, 2, '0'),
       '000-00-' || lpad(i::text, 4, '0')
FROM generate_series(1, 8) i;
-- unguessable / rls_silent / plain / 9 rows
CREATE TABLE public."tbl_8fc310b9" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_8fc310b9" (label, value)
SELECT 'tbl_8fc310b9-' || i, 'value-' || i FROM generate_series(1, 9) i;
-- unguessable / open / plain / 3 rows
CREATE TABLE public."tbl_d82e4466" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_d82e4466" (label, value)
SELECT 'tbl_d82e4466-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / norights / plain / 4 rows
CREATE TABLE public."tbl_f079bde3" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_f079bde3" (label, value)
SELECT 'tbl_f079bde3-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / open / plain / 5 rows
CREATE TABLE public."tbl_e7038292" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_e7038292" (label, value)
SELECT 'tbl_e7038292-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / rls_silent / plain / 6 rows
CREATE TABLE public."tbl_a3ecb6b4" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_a3ecb6b4" (label, value)
SELECT 'tbl_a3ecb6b4-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- unguessable / open / plain / 7 rows
CREATE TABLE public."tbl_2d8c31f1" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_2d8c31f1" (label, value)
SELECT 'tbl_2d8c31f1-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- unguessable / norights / plain / 8 rows
CREATE TABLE public."tbl_75f197c1" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_75f197c1" (label, value)
SELECT 'tbl_75f197c1-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- unguessable / open / credential / 9 rows
CREATE TABLE public."tbl_465c6bd5" (
  id bigserial PRIMARY KEY,
  email text NOT NULL,
  password_hash text NOT NULL,
  api_key text NOT NULL,
  totp_secret text
);
INSERT INTO public."tbl_465c6bd5" (email, password_hash, api_key, totp_secret)
SELECT 'user' || i || '@example.invalid',
       '$2b$12$' || lpad(i::text, 53, '0'),
       'sk_test_' || md5('tbl_465c6bd5' || i),
       'JBSWY3DPEHPK3PX' || i
FROM generate_series(1, 9) i;
-- unguessable / rls_silent / plain / 3 rows
CREATE TABLE public."tbl_10a7dae1" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_10a7dae1" (label, value)
SELECT 'tbl_10a7dae1-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / open / plain / 4 rows
CREATE TABLE public."tbl_24979ebd" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_24979ebd" (label, value)
SELECT 'tbl_24979ebd-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / norights / plain / 5 rows
CREATE TABLE public."tbl_6f977d59" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_6f977d59" (label, value)
SELECT 'tbl_6f977d59-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / open / plain / 6 rows
CREATE TABLE public."tbl_086b398b" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_086b398b" (label, value)
SELECT 'tbl_086b398b-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- unguessable / rls_silent / plain / 7 rows
CREATE TABLE public."tbl_ce7ddd42" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_ce7ddd42" (label, value)
SELECT 'tbl_ce7ddd42-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- unguessable / open / card / 8 rows
CREATE TABLE public."tbl_554a6bbb" (
  id bigserial PRIMARY KEY,
  card_number text NOT NULL,
  card_expiry text,
  iban text,
  billing_email text
);
INSERT INTO public."tbl_554a6bbb" (card_number, card_expiry, iban, billing_email)
SELECT (ARRAY['4111111111111111','5555555555554444','378282246310005','6011111111111117'])[1 + (i % 4)],
       '0' || (1 + (i % 9)) || '/29',
       (ARRAY['DE89370400440532013000','GB82WEST12345698765432','NL91ABNA0417164300'])[1 + (i % 3)],
       'billing' || i || '@example.invalid'
FROM generate_series(1, 8) i;
-- unguessable / norights / plain / 9 rows
CREATE TABLE public."tbl_58134642" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_58134642" (label, value)
SELECT 'tbl_58134642-' || i, 'value-' || i FROM generate_series(1, 9) i;
-- unguessable / open / plain / 3 rows
CREATE TABLE public."tbl_f4136804" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_f4136804" (label, value)
SELECT 'tbl_f4136804-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / rls_silent / plain / 4 rows
CREATE TABLE public."tbl_4894c979" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tbl_4894c979" (label, value)
SELECT 'tbl_4894c979-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / open / plain / 5 rows
CREATE TABLE public."kv_e0a5ab" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_e0a5ab" (label, value)
SELECT 'kv_e0a5ab-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / norights / plain / 6 rows
CREATE TABLE public."kv_678db6" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_678db6" (label, value)
SELECT 'kv_678db6-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- unguessable / open / pii / 7 rows
CREATE TABLE public."kv_b9d403" (
  id bigserial PRIMARY KEY,
  full_name text NOT NULL,
  email text NOT NULL,
  phone_number text,
  national_id text
);
INSERT INTO public."kv_b9d403" (full_name, email, phone_number, national_id)
SELECT (ARRAY['Dana Whitfield','Marcus Oyelaran','Priya Raghunathan','Tomas Berg','Elena Marchetti'])[1 + (i % 5)] || ' ' || i,
       'person' || i || '@example.invalid',
       '+1-555-02' || lpad(i::text, 2, '0'),
       '000-00-' || lpad(i::text, 4, '0')
FROM generate_series(1, 7) i;
-- unguessable / rls_silent / plain / 8 rows
CREATE TABLE public."kv_b02f8c" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_b02f8c" (label, value)
SELECT 'kv_b02f8c-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- unguessable / open / plain / 9 rows
CREATE TABLE public."kv_7c5799" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_7c5799" (label, value)
SELECT 'kv_7c5799-' || i, 'value-' || i FROM generate_series(1, 9) i;
-- unguessable / norights / plain / 3 rows
CREATE TABLE public."kv_f362f4" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_f362f4" (label, value)
SELECT 'kv_f362f4-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / open / plain / 4 rows
CREATE TABLE public."kv_70fc59" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_70fc59" (label, value)
SELECT 'kv_70fc59-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / rls_silent / plain / 5 rows
CREATE TABLE public."kv_c9706f" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_c9706f" (label, value)
SELECT 'kv_c9706f-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / open / plain / 6 rows
CREATE TABLE public."kv_a32006" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_a32006" (label, value)
SELECT 'kv_a32006-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- unguessable / norights / plain / 7 rows
CREATE TABLE public."kv_a02a9f" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_a02a9f" (label, value)
SELECT 'kv_a02a9f-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- unguessable / open / credential / 8 rows
CREATE TABLE public."kv_7da565" (
  id bigserial PRIMARY KEY,
  email text NOT NULL,
  password_hash text NOT NULL,
  api_key text NOT NULL,
  totp_secret text
);
INSERT INTO public."kv_7da565" (email, password_hash, api_key, totp_secret)
SELECT 'user' || i || '@example.invalid',
       '$2b$12$' || lpad(i::text, 53, '0'),
       'sk_test_' || md5('kv_7da565' || i),
       'JBSWY3DPEHPK3PX' || i
FROM generate_series(1, 8) i;
-- unguessable / rls_silent / plain / 9 rows
CREATE TABLE public."kv_9aaa45" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_9aaa45" (label, value)
SELECT 'kv_9aaa45-' || i, 'value-' || i FROM generate_series(1, 9) i;
-- unguessable / open / plain / 3 rows
CREATE TABLE public."kv_4d05bc" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_4d05bc" (label, value)
SELECT 'kv_4d05bc-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / norights / plain / 4 rows
CREATE TABLE public."kv_df5584" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_df5584" (label, value)
SELECT 'kv_df5584-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / open / plain / 5 rows
CREATE TABLE public."kv_52b29a" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_52b29a" (label, value)
SELECT 'kv_52b29a-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / rls_silent / plain / 6 rows
CREATE TABLE public."kv_e8c133" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_e8c133" (label, value)
SELECT 'kv_e8c133-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- unguessable / open / card / 7 rows
CREATE TABLE public."kv_089941" (
  id bigserial PRIMARY KEY,
  card_number text NOT NULL,
  card_expiry text,
  iban text,
  billing_email text
);
INSERT INTO public."kv_089941" (card_number, card_expiry, iban, billing_email)
SELECT (ARRAY['4111111111111111','5555555555554444','378282246310005','6011111111111117'])[1 + (i % 4)],
       '0' || (1 + (i % 9)) || '/29',
       (ARRAY['DE89370400440532013000','GB82WEST12345698765432','NL91ABNA0417164300'])[1 + (i % 3)],
       'billing' || i || '@example.invalid'
FROM generate_series(1, 7) i;
-- unguessable / norights / plain / 8 rows
CREATE TABLE public."kv_7e3866" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_7e3866" (label, value)
SELECT 'kv_7e3866-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- unguessable / open / plain / 9 rows
CREATE TABLE public."kv_80ceac" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_80ceac" (label, value)
SELECT 'kv_80ceac-' || i, 'value-' || i FROM generate_series(1, 9) i;
-- unguessable / rls_silent / plain / 3 rows
CREATE TABLE public."kv_33fb26" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_33fb26" (label, value)
SELECT 'kv_33fb26-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / open / plain / 4 rows
CREATE TABLE public."kv_b1cf27" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_b1cf27" (label, value)
SELECT 'kv_b1cf27-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / norights / plain / 5 rows
CREATE TABLE public."kv_fd3174" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_fd3174" (label, value)
SELECT 'kv_fd3174-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / open / pii / 6 rows
CREATE TABLE public."kv_3dc18d" (
  id bigserial PRIMARY KEY,
  full_name text NOT NULL,
  email text NOT NULL,
  phone_number text,
  national_id text
);
INSERT INTO public."kv_3dc18d" (full_name, email, phone_number, national_id)
SELECT (ARRAY['Dana Whitfield','Marcus Oyelaran','Priya Raghunathan','Tomas Berg','Elena Marchetti'])[1 + (i % 5)] || ' ' || i,
       'person' || i || '@example.invalid',
       '+1-555-02' || lpad(i::text, 2, '0'),
       '000-00-' || lpad(i::text, 4, '0')
FROM generate_series(1, 6) i;
-- unguessable / rls_silent / plain / 7 rows
CREATE TABLE public."kv_4b24c4" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_4b24c4" (label, value)
SELECT 'kv_4b24c4-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- unguessable / open / plain / 8 rows
CREATE TABLE public."kv_97d1bb" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."kv_97d1bb" (label, value)
SELECT 'kv_97d1bb-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- unguessable / norights / plain / 9 rows
CREATE TABLE public."sporule_ledger" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."sporule_ledger" (label, value)
SELECT 'sporule_ledger-' || i, 'value-' || i FROM generate_series(1, 9) i;
-- unguessable / open / plain / 3 rows
CREATE TABLE public."quenchless_intake" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."quenchless_intake" (label, value)
SELECT 'quenchless_intake-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / rls_silent / plain / 4 rows
CREATE TABLE public."halidom_registry" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."halidom_registry" (label, value)
SELECT 'halidom_registry-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / open / plain / 5 rows
CREATE TABLE public."farrago_queue" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."farrago_queue" (label, value)
SELECT 'farrago_queue-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / norights / plain / 6 rows
CREATE TABLE public."welkin_vault" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."welkin_vault" (label, value)
SELECT 'welkin_vault-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- unguessable / open / credential / 7 rows
CREATE TABLE public."pettifog_archive" (
  id bigserial PRIMARY KEY,
  email text NOT NULL,
  password_hash text NOT NULL,
  api_key text NOT NULL,
  totp_secret text
);
INSERT INTO public."pettifog_archive" (email, password_hash, api_key, totp_secret)
SELECT 'user' || i || '@example.invalid',
       '$2b$12$' || lpad(i::text, 53, '0'),
       'sk_test_' || md5('pettifog_archive' || i),
       'JBSWY3DPEHPK3PX' || i
FROM generate_series(1, 7) i;
-- unguessable / rls_silent / plain / 8 rows
CREATE TABLE public."tarnation_shard" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."tarnation_shard" (label, value)
SELECT 'tarnation_shard-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- unguessable / open / plain / 9 rows
CREATE TABLE public."obelus_index" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."obelus_index" (label, value)
SELECT 'obelus_index-' || i, 'value-' || i FROM generate_series(1, 9) i;
-- unguessable / norights / plain / 3 rows
CREATE TABLE public."susurrus_cache" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."susurrus_cache" (label, value)
SELECT 'susurrus_cache-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / open / plain / 4 rows
CREATE TABLE public."gallimaufry_store" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."gallimaufry_store" (label, value)
SELECT 'gallimaufry_store-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / rls_silent / plain / 5 rows
CREATE TABLE public."zxb9afef8f7f" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zxb9afef8f7f" (label, value)
SELECT 'zxb9afef8f7f-' || i, 'value-' || i FROM generate_series(1, 5) i;
-- unguessable / open / card / 6 rows
CREATE TABLE public."zx74a4150338" (
  id bigserial PRIMARY KEY,
  card_number text NOT NULL,
  card_expiry text,
  iban text,
  billing_email text
);
INSERT INTO public."zx74a4150338" (card_number, card_expiry, iban, billing_email)
SELECT (ARRAY['4111111111111111','5555555555554444','378282246310005','6011111111111117'])[1 + (i % 4)],
       '0' || (1 + (i % 9)) || '/29',
       (ARRAY['DE89370400440532013000','GB82WEST12345698765432','NL91ABNA0417164300'])[1 + (i % 3)],
       'billing' || i || '@example.invalid'
FROM generate_series(1, 6) i;
-- unguessable / norights / plain / 7 rows
CREATE TABLE public."zxf0a4500081" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zxf0a4500081" (label, value)
SELECT 'zxf0a4500081-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- unguessable / open / plain / 8 rows
CREATE TABLE public."zxd703f71d9a" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zxd703f71d9a" (label, value)
SELECT 'zxd703f71d9a-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- unguessable / rls_silent / plain / 9 rows
CREATE TABLE public."zx0e6f877b4b" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zx0e6f877b4b" (label, value)
SELECT 'zx0e6f877b4b-' || i, 'value-' || i FROM generate_series(1, 9) i;
-- unguessable / open / plain / 3 rows
CREATE TABLE public."zx0f3d3b5c27" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zx0f3d3b5c27" (label, value)
SELECT 'zx0f3d3b5c27-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / norights / plain / 4 rows
CREATE TABLE public."zx6a6917eae3" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zx6a6917eae3" (label, value)
SELECT 'zx6a6917eae3-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / open / pii / 5 rows
CREATE TABLE public."zx9ec4126434" (
  id bigserial PRIMARY KEY,
  full_name text NOT NULL,
  email text NOT NULL,
  phone_number text,
  national_id text
);
INSERT INTO public."zx9ec4126434" (full_name, email, phone_number, national_id)
SELECT (ARRAY['Dana Whitfield','Marcus Oyelaran','Priya Raghunathan','Tomas Berg','Elena Marchetti'])[1 + (i % 5)] || ' ' || i,
       'person' || i || '@example.invalid',
       '+1-555-02' || lpad(i::text, 2, '0'),
       '000-00-' || lpad(i::text, 4, '0')
FROM generate_series(1, 5) i;
-- unguessable / rls_silent / plain / 6 rows
CREATE TABLE public."zx1bef9c5c90" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zx1bef9c5c90" (label, value)
SELECT 'zx1bef9c5c90-' || i, 'value-' || i FROM generate_series(1, 6) i;
-- unguessable / open / plain / 7 rows
CREATE TABLE public."zx039567d963" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zx039567d963" (label, value)
SELECT 'zx039567d963-' || i, 'value-' || i FROM generate_series(1, 7) i;
-- unguessable / norights / plain / 8 rows
CREATE TABLE public."zx51b5d3bc08" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zx51b5d3bc08" (label, value)
SELECT 'zx51b5d3bc08-' || i, 'value-' || i FROM generate_series(1, 8) i;
-- unguessable / open / plain / 9 rows
CREATE TABLE public."zxb8970ad063" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zxb8970ad063" (label, value)
SELECT 'zxb8970ad063-' || i, 'value-' || i FROM generate_series(1, 9) i;
-- unguessable / rls_silent / plain / 3 rows
CREATE TABLE public."zx3ef932f351" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zx3ef932f351" (label, value)
SELECT 'zx3ef932f351-' || i, 'value-' || i FROM generate_series(1, 3) i;
-- unguessable / open / plain / 4 rows
CREATE TABLE public."zx87e244198b" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zx87e244198b" (label, value)
SELECT 'zx87e244198b-' || i, 'value-' || i FROM generate_series(1, 4) i;
-- unguessable / norights / plain / 5 rows
CREATE TABLE public."zx43273989b0" (
  id bigserial PRIMARY KEY,
  label text NOT NULL,
  value text,
  created_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO public."zx43273989b0" (label, value)
SELECT 'zx43273989b0-' || i, 'value-' || i FROM generate_series(1, 5) i;

-- ---------------------------------------------------------------------------
-- Privileges. SELECT only, everywhere: this project measures DISCOVERY, and
-- write verbs are project 01's axis. Keeping writes out means a failure here
-- can only be a failure to find or to classify.
-- ---------------------------------------------------------------------------
GRANT SELECT ON public."users" TO anon, authenticated;
GRANT SELECT ON public."orders" TO anon, authenticated;
GRANT SELECT ON public."sessions" TO anon, authenticated;
GRANT SELECT ON public."payments" TO anon, authenticated;
GRANT SELECT ON public."messages" TO anon, authenticated;
GRANT SELECT ON public."products" TO anon, authenticated;
GRANT SELECT ON public."comments" TO anon, authenticated;
GRANT SELECT ON public."posts" TO anon, authenticated;
GRANT SELECT ON public."files" TO anon, authenticated;
GRANT SELECT ON public."events" TO anon, authenticated;
GRANT SELECT ON public."t1" TO anon, authenticated;
GRANT SELECT ON public."t2" TO anon, authenticated;
GRANT SELECT ON public."t3" TO anon, authenticated;
GRANT SELECT ON public."t5" TO anon, authenticated;
GRANT SELECT ON public."t6" TO anon, authenticated;
GRANT SELECT ON public."t7" TO anon, authenticated;
GRANT SELECT ON public."t9" TO anon, authenticated;
GRANT SELECT ON public."t10" TO anon, authenticated;
GRANT SELECT ON public."t11" TO anon, authenticated;
GRANT SELECT ON public."data" TO anon, authenticated;
GRANT SELECT ON public."payload" TO anon, authenticated;
GRANT SELECT ON public."tmp_1" TO anon, authenticated;
GRANT SELECT ON public."staging_a" TO anon, authenticated;
GRANT SELECT ON public."staging_b" TO anon, authenticated;
GRANT SELECT ON public."misc" TO anon, authenticated;
GRANT SELECT ON public."tbl_0e1eb483" TO anon, authenticated;
GRANT SELECT ON public."tbl_75bea5ae" TO anon, authenticated;
GRANT SELECT ON public."tbl_685ec957" TO anon, authenticated;
GRANT SELECT ON public."tbl_8bd78e19" TO anon, authenticated;
GRANT SELECT ON public."tbl_8f676a9d" TO anon, authenticated;
GRANT SELECT ON public."tbl_1c6d981c" TO anon, authenticated;
GRANT SELECT ON public."tbl_3d461043" TO anon, authenticated;
GRANT SELECT ON public."tbl_f412a711" TO anon, authenticated;
GRANT SELECT ON public."tbl_4c521ea9" TO anon, authenticated;
GRANT SELECT ON public."tbl_beef7050" TO anon, authenticated;
GRANT SELECT ON public."tbl_8fc310b9" TO anon, authenticated;
GRANT SELECT ON public."tbl_d82e4466" TO anon, authenticated;
GRANT SELECT ON public."tbl_e7038292" TO anon, authenticated;
GRANT SELECT ON public."tbl_a3ecb6b4" TO anon, authenticated;
GRANT SELECT ON public."tbl_2d8c31f1" TO anon, authenticated;
GRANT SELECT ON public."tbl_465c6bd5" TO anon, authenticated;
GRANT SELECT ON public."tbl_10a7dae1" TO anon, authenticated;
GRANT SELECT ON public."tbl_24979ebd" TO anon, authenticated;
GRANT SELECT ON public."tbl_086b398b" TO anon, authenticated;
GRANT SELECT ON public."tbl_ce7ddd42" TO anon, authenticated;
GRANT SELECT ON public."tbl_554a6bbb" TO anon, authenticated;
GRANT SELECT ON public."tbl_f4136804" TO anon, authenticated;
GRANT SELECT ON public."tbl_4894c979" TO anon, authenticated;
GRANT SELECT ON public."kv_e0a5ab" TO anon, authenticated;
GRANT SELECT ON public."kv_b9d403" TO anon, authenticated;
GRANT SELECT ON public."kv_b02f8c" TO anon, authenticated;
GRANT SELECT ON public."kv_7c5799" TO anon, authenticated;
GRANT SELECT ON public."kv_70fc59" TO anon, authenticated;
GRANT SELECT ON public."kv_c9706f" TO anon, authenticated;
GRANT SELECT ON public."kv_a32006" TO anon, authenticated;
GRANT SELECT ON public."kv_7da565" TO anon, authenticated;
GRANT SELECT ON public."kv_9aaa45" TO anon, authenticated;
GRANT SELECT ON public."kv_4d05bc" TO anon, authenticated;
GRANT SELECT ON public."kv_52b29a" TO anon, authenticated;
GRANT SELECT ON public."kv_e8c133" TO anon, authenticated;
GRANT SELECT ON public."kv_089941" TO anon, authenticated;
GRANT SELECT ON public."kv_80ceac" TO anon, authenticated;
GRANT SELECT ON public."kv_33fb26" TO anon, authenticated;
GRANT SELECT ON public."kv_b1cf27" TO anon, authenticated;
GRANT SELECT ON public."kv_3dc18d" TO anon, authenticated;
GRANT SELECT ON public."kv_4b24c4" TO anon, authenticated;
GRANT SELECT ON public."kv_97d1bb" TO anon, authenticated;
GRANT SELECT ON public."quenchless_intake" TO anon, authenticated;
GRANT SELECT ON public."halidom_registry" TO anon, authenticated;
GRANT SELECT ON public."farrago_queue" TO anon, authenticated;
GRANT SELECT ON public."pettifog_archive" TO anon, authenticated;
GRANT SELECT ON public."tarnation_shard" TO anon, authenticated;
GRANT SELECT ON public."obelus_index" TO anon, authenticated;
GRANT SELECT ON public."gallimaufry_store" TO anon, authenticated;
GRANT SELECT ON public."zxb9afef8f7f" TO anon, authenticated;
GRANT SELECT ON public."zx74a4150338" TO anon, authenticated;
GRANT SELECT ON public."zxd703f71d9a" TO anon, authenticated;
GRANT SELECT ON public."zx0e6f877b4b" TO anon, authenticated;
GRANT SELECT ON public."zx0f3d3b5c27" TO anon, authenticated;
GRANT SELECT ON public."zx9ec4126434" TO anon, authenticated;
GRANT SELECT ON public."zx1bef9c5c90" TO anon, authenticated;
GRANT SELECT ON public."zx039567d963" TO anon, authenticated;
GRANT SELECT ON public."zxb8970ad063" TO anon, authenticated;
GRANT SELECT ON public."zx3ef932f351" TO anon, authenticated;
GRANT SELECT ON public."zx87e244198b" TO anon, authenticated;

-- ---------------------------------------------------------------------------
-- RLS with no policy attached. These hold a SELECT grant, so the request
-- succeeds; the policy set is empty, so the result is [] forever. They are
-- NOT exposed, and they are NOT absent.
-- ---------------------------------------------------------------------------
ALTER TABLE public."orders" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."sessions" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."payments" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."messages" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."t2" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."t6" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."t10" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."payload" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."staging_b" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."tbl_75bea5ae" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."tbl_8f676a9d" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."tbl_f412a711" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."tbl_8fc310b9" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."tbl_a3ecb6b4" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."tbl_10a7dae1" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."tbl_ce7ddd42" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."tbl_4894c979" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."kv_b02f8c" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."kv_c9706f" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."kv_9aaa45" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."kv_e8c133" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."kv_33fb26" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."kv_4b24c4" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."halidom_registry" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."tarnation_shard" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."zxb9afef8f7f" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."zx0e6f877b4b" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."zx1bef9c5c90" ENABLE ROW LEVEL SECURITY;
ALTER TABLE public."zx3ef932f351" ENABLE ROW LEVEL SECURITY;

-- No INSERT/UPDATE/DELETE is granted anywhere in this project.
